# @package _global_

defaults:
  - override /dataset: nuscene
  - override /model/encoder: epipolar
  - override /model/encoder/backbone: dino
  - override /loss: [mse, lpips]

wandb:
  name: nuscene
  tags: [seed, 256x256]

dataset:
  image_shape: [256, 256]
  roots: [dataset/nuscene]

data_loader:
  train:
    batch_size: 1 # 7 

trainer:
  max_steps: 400_001

test:
  depth_mode: depth
  output_path: outputs/test/time_df4
  compute_scores: true